{% layout none %}
<div id="dynamic-product-grid">
  {% for product in collection.products limit: 9 %}
    <div class="bg-white border border-gray-200 rounded-lg overflow-hidden shadow-sm">
      <a href="{{ product.url }}" class="block">
        <div class="aspect-square bg-gray-100 flex items-center justify-center overflow-hidden">
          {% if product.featured_image != blank %}
            <img 
              src="{{ product.featured_image | image_url: width: 400 }}" 
              alt="{{ product.title | escape }}" 
              loading="lazy"
              class="w-full h-full object-cover"
              width="{{ product.featured_image.width }}"
              height="{{ product.featured_image.height }}"
            >
          {% else %}
            {{ 'product-1' | placeholder_svg_tag: 'w-full h-full bg-gray-200' }}
          {% endif %}
        </div>
        <div class="p-4 text-center">
          <h3 class="text-sm font-bold text-gray-800 truncate">{{ product.title }}</h3>
          <p class="text-sm text-gray-600 font-medium mt-1">{{ product.price | money }}</p>
        </div>
      </a>
    </div>
  {% else %}
    <p class="col-span-3 text-center py-12 text-gray-500">No products found in this collection.</p>
  {% endfor %}
</div>